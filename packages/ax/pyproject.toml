[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agentcy-ax"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["agentcy-hands"]

[tool.setuptools.packages.find]
where = ["src"]
