# Recursive framework

The framework can classify itself. Buildability evaluates a Spec. A Spec
that describes buildability is a valid input. This document is that Spec's
declaration; the evidence is that `python -m buildability.classify mesh/specs_uci`
runs and reports regimes.
