# model_composition

## Purpose
Combine two computational models into one.

## Interface
models.compose.combine(a, b) -> ComposedModel with solver, prover, resolver.

state: done
evidence-file: packages/buildability/src/buildability/models/compose.py
